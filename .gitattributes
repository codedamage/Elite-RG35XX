# Auto detect text files and perform LF normalization
* text=auto

# Scripts + C sources MUST be LF — they run inside the Linux toolchain container.
*.sh    text eol=lf
*.c     text eol=lf
*.h     text eol=lf
Dockerfile text eol=lf
